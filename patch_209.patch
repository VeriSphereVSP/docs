diff --git a/vsrc/verisphere_vsp.py b/vsrc/verisphere_vsp.py
--- a/vsrc/verisphere_vsp.py
+++ b/vsrc/verisphere_vsp.py
@@ -24,13 +24,16 @@
 
     def get_dvs_info(self):
         """
-        Get distributed virtual switch info.
+        Get dvs info by dvSwitchManager.
+
+        Returns:
+            vim.dvs.GetDvsGetInfoResult: DVS information result
+
+        Raises:
+            Exception: If unable to retrieve DVS info.
         """
-        try:
-            return self.si.content.dvSwitchManager.QueryDvsGetInfoEx(
-                self.vspmoid)
-        except Exception as e:
-            log.error("Failed to get dvs info: %s", str(e))
+        task = self.si.content.dvSwitchManager.QueryDvsGetInfoEx_Task(self.vspmoid)
+        return task.wait() if task.result else None
 
     def get_dvses(self):
@@ -103,7 +106,7 @@
 
         # Get all dvs info
         dvinfos = self.get_dvs_info()
-        if not dvinfos:
+        if not dvinfos or not dvinfos.distributedVirtualSwitch:
             log.error("No dvswitches found for vspmoid: %s", self.vspmoid)
             return
 
diff --git a/vtest/vsphere_vsp_test.py b/vtest/vsphere_vsp_test.py
--- a/vtest/vsphere_vsp_test.py
+++ b/vtest/vsphere_vsp_test.py
@@ -25,10 +25,6 @@
         self.assertEqual(self.vsp.get_dvses(), self.dvswitches)
 
     def test_get_dvs_info(self):
-        # TODO: Fix this test. It's not working.
-        #       Current code is returning None but test expects a list
-        #       of dvSwitch objects
-        return
         dvinfos = self.vsp.get_dvs_info()
         self.assertTrue(dvinfos)
         self.assertEqual(len(dvinfos.distributedVirtualSwitch), 2)